# GitHub accounts

## 2026-08-13 — Two accounts, two repos

- **Laravel (`ShamandoraScout`)** — GitHub `kiroloskhela`. Remote: `https://github.com/kiroloskhela/ShamandoraScout.git`. Use `kiroloskhela` for commits, `gh`, and push/PR.
- **Flutter mobile app** — GitHub `kirokhela`. Remote: `https://github.com/davidayman11/shamandora.git`. Local copy: `/Users/kiroloskhela/Desktop/scout/shamandora-Mobile-App` (git repo, origin fetched, local `user.name` = `kirokhela`). `kirokhela` has push access. Default branch: `main` (`3878444` new edits).
- `gh` accounts (2026-08-13): both valid. **Active: `kirokhela`**. Switch with `gh auth switch -u kiroloskhela` before Laravel push/PR, and `gh auth switch -u kirokhela` before Flutter push/PR.
